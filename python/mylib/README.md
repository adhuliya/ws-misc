CodeWalker's Python Library
======================

`cw` stands for **CodeWalker**. All Python library modules sit inside this top level module to bring them under a separate namespace and avoid name collisions.
